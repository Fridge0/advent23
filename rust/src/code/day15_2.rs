pub fn solve(input: String) -> i32 {
    let mut boxes = [];
    for entry in input.split(",") {
        boxes.find()
    }
}

fn hash(str: &str) -> i32 {
    if str.len() == 0 {
        return 0;
    }
    return (hash(str[1..]));
}
